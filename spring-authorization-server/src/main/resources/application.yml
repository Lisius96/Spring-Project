server:
  port: 9000

spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/user-registration
    username: root
    #password: ellie&joel
    password: SofiaMySQL
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true